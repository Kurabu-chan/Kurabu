apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- "../../core"

commonLabels:
  app: staging

namespace: staging       

resources:
- namespace.yaml
- host-ingress.yaml
- path-ingress.yaml
- secret.yaml
- db-persistent-volume.yaml

images:
- name: kurabu-api
  newTag: 'latest'
  newName: 'rafaeltab/kurabu.api'